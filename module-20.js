/* function inchToFeet(inch) {
  const feet = inch / 12;
  return feet;
}

const dataToConvert = 60;
const convertInchToFeet = inchToFeet(dataToConvert);
console.log(convertInchToFeet); */

/* function evenOdd(number) {
  if (number % 2 === 0) {
    return true;
  } else {
    return false;
  }
}

const num = 22;
const result = evenOdd(num);
console.log(result);
 */

/* function findOddSum(numbers) {
  let sum = 0;
  const oddNumbers = [];

  for (let i = 0; i < numbers.length; i++) {
    // const index = i;
    // const element = numbers[i];
    // console.log(index, element); 
    if (numbers[i] % 2 === 1) {
      sum = sum + numbers[i];
      oddNumbers.push(numbers[i]);
    }
  }
  return oddNumbers;
}

const myNumbers = [10, 20, 23, 45, 34, 12, 25];
const result = findOddSum(myNumbers);
console.log(result); */
